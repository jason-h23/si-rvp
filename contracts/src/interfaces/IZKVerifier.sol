// SPDX-License-Identifier: MIT
pragma solidity ^0.8.24;

/**
 * @title IZKVerifier
 * @notice ZK proof verification interface
 * @dev Interface for Groth16 proof verification
 *      Actual implementation is auto-generated by snarkjs
 */
interface IZKVerifier {
    /**
     * @notice Groth16 proof verification
     * @param a Proof point A (G1)
     * @param b Proof point B (G2)
     * @param c Proof point C (G1)
     * @param publicInputs Array of public inputs
     * @return valid Whether the proof is valid
     *
     * @dev Expected gas: ~280,000
     *
     * Public inputs structure (MIPS Single Step, snarkjs public-signal
     * order — circuit outputs precede public inputs):
     * - publicInputs[0]: valid (circuit output)
     * - publicInputs[1]: preStateHash (Poseidon hash of the pre-state)
     * - publicInputs[2]: postStateHash (Poseidon hash of the post-state)
     */
    function verifyProof(
        uint256[2] calldata a,
        uint256[2][2] calldata b,
        uint256[2] calldata c,
        uint256[] calldata publicInputs
    ) external view returns (bool valid);
}
