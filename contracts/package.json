{
  "name": "@si-rvp/contracts",
  "version": "0.1.0",
  "description": "SI-RVP smart contracts (DisputeManager, RollupManager, Groth16 verifier, factory/proxy adapters)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:localhost": "hardhat run scripts/deploy.ts --network localhost",
    "verify:sepolia": "hardhat run scripts/verify-contracts.ts --network sepolia",
    "clean": "hardhat clean",
    "live-test:sepolia": "hardhat run scripts/live-test.ts --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@types/chai": "^4.3.11",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.10.0",
    "chai": "^4.3.10",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
